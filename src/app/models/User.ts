export interface User {
  firstName: string;
  lastName: string;
  email: string;
  // age?: number;
  // address?: {
  //   street?: string;
  //   houseNumber?: number;
  //   city?: string;
  // };
  // image?: string;
  isActive?: boolean;
  // balance?: number;
  joined?: any;
  showDetails?: boolean;
}
